name: Build PJSK Zip Package

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Repo'yu Kopyala
      uses: actions/checkout@v4

    - name: ZIP Paketine Dönüştür
      run: |
        Compress-Archive -Path "PJSK_Custom" -DestinationPath "PJSK_Project_Sekai_Player.zip" -Force

    - name: Artifact Olarak Kaydet
      uses: actions/upload-artifact@v4
      with:
        name: PJSK-Custom-Bundle
        path: PJSK_Project_Sekai_Player.zip
